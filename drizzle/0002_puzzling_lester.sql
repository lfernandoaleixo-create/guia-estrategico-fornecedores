CREATE TABLE `viability_sheets` (
	`scope` varchar(96) NOT NULL,
	`supplierId` varchar(191) NOT NULL,
	`data` longtext NOT NULL,
	`createdAt` varchar(40) NOT NULL,
	`updatedAt` varchar(40) NOT NULL
);
